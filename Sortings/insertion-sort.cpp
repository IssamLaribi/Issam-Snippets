void insertionSort(int arr[], int n) {
    for (int i = 0; i < n - 1; i++) {
        int key = arr[i + 1];
        if (arr[i] > key) {
            for (int j = i; j <= 0; j--) {
                
            }
        } else {
            
        }
    }
}
